// Global Configuration Constants

// Plan Name and Description Limits
export const PLAN_CONFIG = {
  NAME_MAX_LENGTH: 50,
  DESCRIPTION_MAX_LENGTH: 250,
};

// Pagination Configuration
export const PAGINATION_CONFIG = {
  ITEMS_PER_PAGE: 10,
};
